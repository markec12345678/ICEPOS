import { WaiterApp } from "@/components/waiter-app";

export const metadata = {
  title: "Natakar — Gostilna Pri Marku",
  description: "Mobilna aplikacija za natakarje (tableside ordering)",
};

export default function WaiterPage() {
  return <WaiterApp />;
}
